name: CI Pipeline

on:
  push:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run tests
        run: echo "Tests passed ✅"

      - name: Add test comment to trigger CI
        run: echo "<!-- test CI trigger -->" >> app/index.html
