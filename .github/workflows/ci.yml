name: Simplify CI workflow for html project

on:
  push:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Simple build log
        run: echo "CI OK 🚀 <!-- test CI trigger -->"
